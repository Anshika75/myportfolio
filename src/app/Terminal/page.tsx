// pages/terminal.tsx (similar pages for other themes)
import React from 'react';
import Head from 'next/head';

const Terminal: React.FC = () => {
  return (
    <div>
      <Head>
        <title>Terminal Mode</title>
      </Head>
      <h1>Terminal Mode</h1>
      {/* Your terminal-specific content goes here */}
      {/* <ThemeSelector /> */}
    </div>
  );
};

export default Terminal;
